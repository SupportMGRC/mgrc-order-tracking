// Dashboard Analytics Init JS
// This is a minimal file to avoid conflicts with the inline scripts
// The actual chart initialization is done in the dashboard.blade.php file

document.addEventListener('DOMContentLoaded', function() {
    // This file is loaded but charts are initialized in the blade template
    console.log('Dashboard analytics initialized');
});